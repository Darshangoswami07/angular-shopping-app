# Copy this file to .env and fill in real values.
# These are embedded into the JS bundle at build time by @ngx-env/builder
# (see angular.json) — do not put secrets here, only the public API base URL.

NG_APP_API_URL=https://angular-shopping-app.onrender.com/api
